# 2026-07-08 工作日志

## 部署方案：对齐 ai-board 模式，挂到 /stock

参考项目 `/Users/xyb/Documents/workspace/ai-board` 的极简部署（纯静态前端 + 原生 Nginx 子路径 + rsync 上传），把本仓库 Web 工作台部署到**同一台服务器 `root@69.5.21.175`、同一域名 `yongbo.online`**，子路径改为 `/stock`。

### 关键配置
- 服务器/域名：`root@69.5.21.175`、域名 `yongbo.online`（ai-board 已解析，无需改 DNS；`/stock` 只是路径）。
- 前端静态目录（服务器）：`/var/www/dsa`，由 Nginx `alias` 映射 `/stock/`。
- 后端：用项目自带 Docker 跑 `server` 服务，绑本机 `127.0.0.1:8000`，由 Nginx 反向代理 `/stock/api/` 对外（不暴露公网 8000）。
- 前端构建：`VITE_BASE_PATH=/stock npm run build`，产物资源路径 `/stock/assets/...`。

### 本次新增/修改文件
- `deploy/nginx.conf`：子路径 `/stock` 静态托管（alias + try_files SPA 回退）+ `/stock/api/` 反代到 `127.0.0.1:8000`（含 SSE）。
- `deploy/deploy.sh`：前端构建 + rsync 上传（参考 ai-board 写法）。
- `deploy/DEPLOY_STEPS.md`：完整部署教程（前端 rsync + 后端 docker compose up -d server + nginx 上传 + 访问）。
- 前端 `apps/dsa-web`：新增 `VITE_BASE_PATH` 环境变量统一驱动 vite base / API 前缀(API_BASE_URL) / 路由 basename / 401 跳转 / 返回首页；默认空，不影响根路径部署。

### 清理（保持单一真源）
用户明确要 ai-board 原生 Nginx 模式，故清理了我上一轮误加的 Docker 一键方案：
- 删除 `docker/nginx.conf`、`scripts/deploy/`。
- 回滚 `docker/docker-compose.yml`（去掉 nginx 服务、去掉 x-common 的 VITE_BASE_PATH args；server `ports` 改为 `127.0.0.1:${API_PORT:-8000}:...`）。
- 回滚 `docker/Dockerfile`（去掉 VITE_BASE_PATH ARG/ENV）。
- 回滚 `.env.example`（去掉 WEBUI_BASE_PATH / NGINX_PORT 段）。

### 验证结论
- 鉴权 Cookie 的 Path 固定为 `/`（api/v1/endpoints/auth.py 的 `_cookie_params`），Nginx 剥离 `/stock/` 前缀不影响登录。
- 本地 `VITE_BASE_PATH=/stock` 构建产物资源路径正确为 `/stock/assets/...`（vite outDir 是项目根 `static/`，不是 `dist/`）。

### ⚠️ 关键坑：nginx 不能用独立 server 块
- 服务器上 `ai-board.conf` 已用 `server_name yongbo.online` 占用 443 主 server 块（certbot 管 HTTPS），且 80 端口块只做 HTTP→HTTPS 跳转。
- 若再建独立 `server { server_name yongbo.online }` 块，nginx 同域名同端口只认第一个，`/stock` 会被忽略（404）。
- **正确做法**：`deploy/nginx.conf` 只放 `/stock` 的 location 片段（不含 server 块），部署为 `/etc/nginx/dsa-locations.conf`，由 `deploy/deploy.sh` 用 python 幂等注入 ai-board.conf 的第一个 server 块（加一行 `include /etc/nginx/dsa-locations.conf;`）。
- `stockIndexLoader.ts` 原来写死 `fetch('/stocks.index.json')`，子路径下会请求根路径 404，已改为 `fetch(\`${BASE_PATH}/stocks.index.json\`)`。

### 最终状态（2026-07-08 已执行）
- ✅ 前端实测上线：`https://yongbo.online/stock/` 返回页面（资源 `/stock/...`）、`/stock/stocks.index.json` 200、`/stock/api/health` 502（反代链路通，仅后端未起）、`/board` 200（无回归）。
- 部署命令：`bash deploy/deploy.sh`（构建→rsync `static/` 到 `/var/www/dsa`→上传 nginx 片段并注入+重载）。
- 后端：服务器 Docker 已装（29.2.1）；项目代码已 rsync 到 `/opt/daily_stock_analysis`（排除 node_modules/.git/本地数据/dsa-desktop/tests/static）；`.env` 需从 `.env.example` 生成并填入 API Key（用户负责填密钥）。
- 后端启动：`cd /opt/daily_stock_analysis && docker compose -f docker/docker-compose.yml up -d server`（读 `/opt/daily_stock_analysis/.env`，绑 127.0.0.1:8000）。
- 后端现状：截至本笔记尚未启动（页面可开但 API 502）。

## 后端构建卡点 + 新交付脚本

### 根因
- `requirements.txt` 原写 `longbridge>=0.2.77`，但公网 PyPI 的 0.2.x 系列最高只到 **0.2.75**（另有无关的 4.x 线）。代码用的是 0.2.x API（`longbridge.openapi`/`Config.from_apikey`/`QuoteContext`），所以已改为 `longbridge>=0.2.75`（已写入仓库 requirements.txt）。`data_provider/__init__.py` eager import longbridge，必须可装。

### 小内存导致构建拖垮整台机
- `69.5.21.175` 实测总内存仅 **1.9G**（用户口头说"运行2G/40G"，疑为 2G 上限的容器跑在 40G 宿主机上）。`docker build` 装数百个重依赖时 CPU/IO 吃满，连 nginx/SSH 都被拖到超时。
- 已在该机建 2G swapfile（fstab 持久化）。
- 已将 `docker/Dockerfile` 的 pip 安装改为 BuildKit 缓存挂载（`RUN --mount=type=cache,target=/root/.cache/pip pip install -r requirements.txt`），中断可续传。

### 新脚本 `deploy/server-deploy.sh`（交付给用户自行在服务器跑）
- 自包含、幂等可重复运行：修 longbridge 约束 → 检查 .env → RAM<4G 自动加 2G swap → 构建镜像（Dockerfile 就地烘焙 `VITE_BASE_PATH=/stock` 仅改一次）→ 从镜像抽 `/app/static` 到 `/var/www/dsa`（宿主机免装 node）→ 写 `/etc/nginx/dsa-locations.conf` 并幂等注入 ai-board.conf → 起 `stock-server` → 健康自检。
- 用法：`bash server-deploy.sh`；强制重建前端 `FORCE_FRONTEND=1`；换目录 `PROJECT_DIR=/path bash server-deploy.sh`。
- 前置：服务器装好 docker/compose v2/nginx；项目在 `/opt/daily_stock_analysis`；`.env` 含 `LLM_ANTHROPIC_API_KEY` + `WEBUI_ENABLED=true`。

### 结论给用户
- 40G 内存完全够（4G+ 即可流畅构建）。若目标机实际被限 2G（非 40G 实例），需把内存上限调到 4G+ 才不会卡；脚本会自动加 swap 兜底但仍慢。

## 持续执行（23:xx 续）
- 上次 `setsid` 后台构建被我的 SSH 断连带死（日志停在凌晨 02:33，镜像未生成）。
- 实测 `69.5.21.175` 仍是 **1.9G 总内存**，确认不是 40G 实例（用户口述 40G 与实际不符）。
- 已把 swap 补到 **4G**（`/swapfile` 2G + `/swapfile2` 2G），清掉旧构建残留，杀掉卡死进程。
- 已把 `deploy/server-deploy.sh` rsync 到服务器 `/opt/daily_stock_analysis/deploy/server-deploy.sh`。
- 23:09 用 `setsid` 彻底脱离会话启动 `bash deploy/server-deploy.sh`，日志 `/tmp/deploy.log`，结束打印 `EXIT_CODE=`。前端已部署会跳过重建，直接进入 Python 重依赖安装（这台 2G 机器会慢，约 10–30 分钟）。
- 监控：后台探针读 `/tmp/deploy.log` + 镜像/容器状态；成功标志日志出现 `对外 HTTPS 状态=200`。

## 用户澄清：40G 是硬盘不是内存
- 用户发来阿里云 ECS 截图：`ecs.e-c1m1.lar...` 共享型，**2vCPU 2GiB**，**系统盘 40 GiB SSD**。
- 结论：之前说的"40G"是**硬盘**，内存确实只有 **2GiB**。这台机器能跑本项目（运行时轻），但"首次构建镜像"装重依赖会慢。用户的"把项目放这台运行"= 就是 `69.5.21.175`，可行。

## 用户明确要求：前后端一起构建、整站可访问
- 不能只起后端，要前端(VITE_BASE_PATH=/stock) + 后端镜像一起构建。
- 关键教训：用 `ssh host 'bash deploy/server-deploy.sh &'` 或 `setsid bash -c "..." &` 内联方式，SSH 会话结束时常被一起带走（日志停 pip 阶段、无 EXIT_CODE、无镜像）。**可靠做法**：在服务器写启动器文件 `deploy/run-bg.sh`（写 STARTED/FINISHED 到 /tmp/deploy_started.txt，deploy 输出到 /tmp/deploy.log），再 `setsid /opt/.../run-bg.sh </dev/null >/dev/null 2>&1 &`，用标记文件确认真的起来。
- 23:48 用 `setsid deploy/run-bg.sh`（含 `export FORCE_FRONTEND=1`）成功脱离运行，进程树确认 buildx 在构建。

## ⚠️ 00:24 服务器重启，构建中断
- 查 `uptime`：00:29 时 "up 5 min" → 机器在 **00:24 左右重启过**。`/tmp/deploy*` 与启动标记全清空；`/swapfile2`（未写 fstab）重启后丢失，swap 回到 2G。
- 前端静态文件在 `/var/www/dsa`（系统盘，重启不丢），前端一直可访问；**真正缺的只有后端镜像**。
- 新策略（00:30 重启构建）：① 补回 `/swapfile2` 并写 `/etc/fstab` 持久化（swap 回到 4G，重启不丢）；② 重写 `run-bg.sh` **去掉 FORCE_FRONTEND**，只构建后端镜像（前端已在线，跳过重建以降低负载、防爆机）；③ 再次 `setsid deploy/run-bg.sh` 脱离运行，STARTED 00:30 确认存活。
- 教训：本机 2G 内存跑重 Docker 构建有重启风险；前端已在线就不必每次重构建。后续若再重启且构建是诱因，应改宿主机 venv 直跑后端或请用户提内存。
